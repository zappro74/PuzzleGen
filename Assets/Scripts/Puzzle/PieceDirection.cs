using UnityEngine;

public enum PieceDirection
{
    //not sure if this is the right way to implement piece direction yet...
    //probably won't be using this just yet anyway
    Top,
    Right,
    Bottom,
    Left
}
