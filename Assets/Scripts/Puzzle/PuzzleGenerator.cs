using System.Collections.Generic;
using UnityEngine;

public class PuzzleGenerator
{
    public List<PieceData> Generate(int rows, int columns, int generations)
    {
        List<PieceData> generatedPieces = new List<PieceData>();
        return generatedPieces;
    }
}